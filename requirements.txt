# NIPS — Network Intrusion Prevention System

# Core
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0

# Deep Learning (optional — for LUCID CNN DDoS detection)
tensorflow>=2.17.0

# Web API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.0
python-multipart>=0.0.6

# Live interception (Linux only — install manually)
# pip install NetfilterQueue

# Offline testing / development (install manually)
# pip install scapy  # for pcap file loading: cli.py test --pcap
